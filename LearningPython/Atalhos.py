"""frase = oi chuchu
frase [3:1:5] # primeiro numero onde inicia a contagem, segundo num termina a contagem , 3° numero onde quantos caracteres pula
frase.count ('u') # conta qtos u tem na frase
frase.count ('u',0,13) #conta qtos u tem na frase a partir do 0 até o 13
frase.find (i) #encontra qual posição está a letra i
frase.find (i) #encontra qual posição esta a letra i a partir do fim
len (frase) # conta qtos caracteres tem a frase
'curso' in frase # responde true or false if have this word
curso.replace ('troca essa', 'por essa')
frase.upper() # deixa tudo maiusculo
frase.lower() # deixa tudo minusculo
frase.capitalize() # Apenas a Primeira letra maiuscula
frase.title() # cada primeira letra de cada palavra maiuscula
frase.strip() # remove os espaços desnecessários no começo e no fim
frase.rstrip() # remove apenas os espaços no fim da frase
frase.lstrip() # remove apenas os espaços no começo da frase
frase.split() # separa todas as palavras como "novas strings"
'-'.join(frase) # junta as palavras com '-'
print("""para textos grandes para não precisar colocar varios prints""")
print(frase.count().format())
import # nome da importação que voce vai usar
n = nome da importação.nome do processo da importação ex: numero = random.randint(0,5)
no .format dentro do {} colocar :.2f para ter ,00 depois do resultado
'!='  contrário de igual '=='
and função para adicionar em contas e opções adicionais 
or para ou outra opção dando opção de diferenciação
int - para números inteiros
str - para conjunto de caracteres
bool - armazena True ou False
list - para agrupar um conjunto de elementos
tupla - semelhante ao tipo list, porém, imutável
dic - para agrupar elementos que serão recuperados por uma chave
, and'') - junta a linha de baixo na continuidade da linha de cima
== significa igual
!= significa diferente
print('{:=^40}'.format('Lojas MM')) fica assim ================Lojas MM================
"""

